---
description: Define what to build before implementation
---

Define the requested work before building it.

First, invoke the skill tool to load the relevant skills:

```text
skill({ name: 'using-agent-skills' })
skill({ name: 'idea-refine' })
skill({ name: 'spec-driven-development' })
```

Then follow the skill instructions to refine the idea and produce a concise spec with acceptance criteria.

<user-request>
$ARGUMENTS
</user-request>
