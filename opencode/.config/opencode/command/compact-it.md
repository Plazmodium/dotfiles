---
description: Summarize the conversation for continuing in a new chat
---

Summarize the current conversation into a compact handoff for a future agent session.

First, invoke the skill tool to load the compact-it skill:

```text
skill({ name: 'compact-it' })
```

Then follow the skill instructions to produce the compaction summary.

<user-request>
$ARGUMENTS
</user-request>
