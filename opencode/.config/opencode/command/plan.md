---
description: Break a spec into small, verifiable implementation tasks
---

Create a reviewable implementation plan.

First, invoke the skill tool to load the relevant skills:

```text
skill({ name: 'using-agent-skills' })
skill({ name: 'planning-and-task-breakdown' })
```

Then follow the skill instructions to produce ordered tasks with acceptance criteria and verification checks.

<user-request>
$ARGUMENTS
</user-request>
