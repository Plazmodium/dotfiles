---
description: Simplify code safely while preserving behavior
---

Simplify the requested code without changing behavior.

First, invoke the skill tool to load the relevant skills:

```text
skill({ name: 'using-agent-skills' })
skill({ name: 'code-simplification' })
```

Then follow the skill instructions to understand existing behavior, simplify in small steps, and verify preservation.

<user-request>
$ARGUMENTS
</user-request>
